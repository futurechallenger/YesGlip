<extend src="layout.jael">
  <block name="content">
    <i if=values == null>There are no values</i>
    <ul if=values != null>
      <li for-each=params as="el">
        <span>{{el['k']}}</span><span>{{el['v']}}</span>
      </li>
    </ul>
    <div>{{title}}</div>
  </block>
</extend>
